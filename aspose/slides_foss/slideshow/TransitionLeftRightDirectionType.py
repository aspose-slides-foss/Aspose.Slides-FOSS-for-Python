from __future__ import annotations
from enum import Enum
class TransitionLeftRightDirectionType(Enum):
    """Specifies a direction restricted to the values of left and right."""
    LEFT = 'Left'
    RIGHT = 'Right'
